# 基于国产算力的推理引擎性能基准测试方案（完整版）

## 1. 引言

本项目从 vLLM 稳定分支 fork 而来，目标是在国产算力（如昇腾 NPU）上进行推理引擎性能优化。为确保每一次代码变更都带来正向的性能收益，需要建立一套自动化 Benchmark 体系，并将其作为 PR 合并的强制性门禁。然而，直接对所有目标模型和参数进行全量遍历将导致运行时间过长，严重影响开发效率。本报告综合 vLLM 社区与 vllm-ascend 项目的实际做法，提出一套兼顾质量与效率的**三层分层 Benchmark 方案**，并涵盖标签触发、环境标准化、数据稳定性保障、权限控制及长期维护等配套实践。

## 2. 业界优秀实践参考

### 2.1 vLLM 社区的分层体系

vLLM 明确区分两类性能测试：

- **Performance Benchmarks**：在 PR 被手动打上 `perf-benchmarks` 和 `ready` 标签时触发，用于验证新变更在各种工作负载下的性能影响，是 PR 级别的快速门禁。
- **Nightly Benchmarks**：通过 `perf-benchmarks` 和 `nightly-benchmarks` 标签或定时任务触发，在全量大参数模型上与其他推理引擎（TGI、TRT-LLM 等）进行横向对比，并提供公开性能看板。

该体系的核心设计是：不在 PR 提交时自动运行全量测试，而是通过**标签驱动**的按需触发，将“快速反馈”与“全面验证”解耦。

### 2.2 vllm-ascend 的多级流水线

vllm-ascend 在 GitHub Actions 上实现了更精细的流水线：

- **Main Tests**：每次 PR 或 Push 自动运行，包含单元测试和端到端测试，验证基本功能。
- **Accuracy Testing / Performance Benchmarks**：通过定时任务（每日 2 次）或 PR 标签手动触发，在指定 NPU 资源上运行。性能测试使用 JSON 配置文件定义测试矩阵（延迟、吞吐、服务测试），支持 `/nightly` 评论命令来按需指定测试子集。
- **环境管理**：Benchmark 脚本自动进行 NPU 状态检查、残留进程清理、健康检查轮询等，确保测试环境的一致性和纯净性。

vllm-ascend 明确将性能测试排除在常规 CI 路径之外，避免拖慢日常开发节奏，这一理念值得直接借鉴。

## 3. 核心方案：三层 Benchmark 体系

### 3.1 总体架构

```
┌──────────────────────────┐
│       PR 提交             │
└────────────┬─────────────┘
             │ 路径过滤 & 标签自动/手动触发
             ▼
┌──────────────────────────┐
│  L1: Smoke Benchmark    │  ≤10 分钟
│  1个小模型 + 核心参数   │  PASS/FAIL
└────────────┬─────────────┘
             │ 通过
             ▼
┌──────────────────────────┐
│  L2: 专项验证（按需）    │  30~60 分钟
│  架构抽样 + 极限参数    │  与L1共同构成门禁
└────────────┬─────────────┘
             │ 通过
             ▼
┌──────────────────────────┐
│  人工 Review + Merge     │
└────────────┬─────────────┘
             │ 合并到 main 后不触发 L3
             ▼
┌──────────────────────────┐
│  L3: Comprehensive       │  数小时
│  全量模型 × 参数矩阵    │  看板 + 回归告警
│  (定时/手动触发)        │  更新基线
└──────────────────────────┘
```

### 3.2 L1：PR 门禁快速验证（Smoke Benchmark）

- **定位**：每次 PR 的默认必过门禁，提供最基础、最快速的性能回归反馈。
- **触发方式**：由 PR 自动触发（路径过滤：仅文档、注释等非代码变更的 PR 可跳过）。同时支持通过 `perf-benchmarks` 标签手动重跑。
- **模型选择**：仅选取**一个最具代表性的小模型**（如 Qwen2.5-7B）。选择原则是架构与主优化目标一致，且能覆盖 PagedAttention、Continuous Batching 等核心算子。为加快速度，可使用 `dummy` 权重（不加载真实模型参数），仅测试框架性能。
- **参数精简**：从完整配置中抽取最核心组合。例如：
    - 延迟测试：固定输入长度 1024、输出长度 128，预热 3 次、测量 10 次。
    - 吞吐测试：`num_prompts: 100`，使用 dummy 数据。
    - 服务测试：仅测试 `qps=4` 一个压力点。
- **对比基线**：与预置的基线性能数据（来自最新一次 L3 任务）对比，退化超过阈值（如 5%）则**阻止合并**。

### 3.3 L2：专项验证层（Targeted Verification）

引入 L2 层旨在填补 L1（覆盖面太窄）与 L3（周期太长、成本太高）之间的空白。当代码改动触及关键路径时，L2 能够提供比 L1 更深、但又比 L3 更快的验证，确保高风险变更在合入前被有效拦截。

- **定位**：L2 是对 L3 模型集的科学抽样，目的是在有限时间内验证本次变更对**所有代表性架构和规模**的影响。它并不是 L1 的后续步骤，而是与 L1 并行构成的门禁条件：当 L2 被触发时，PR 必须同时通过 L1 和 L2 才能合入。
- **触发方式（按需触发，非全员）**：
    - **代码路径自动触发（推荐）**：当 PR 修改了核心代码目录时，CI 自动启动 L2。典型的触发路径包括算子库（`csrc/`）、核心推理逻辑（`vllm_ascend/`）、注意力机制实现等。修改文档、示例等路径则不会触发。
    - **手动精准触发**：开发者或审查者可通过 PR 评论命令（如 `/perf-test qwen3-30b-a3b`）手动指定运行 L3 矩阵中的某个或某几个测试项，提供最大灵活性。
- **模型选择（源于 L3，动态抽样）**：
    - **架构抽样**：从 L3 覆盖的每种模型架构（如 Dense、MoE、Vision-Language）中各选取一个中型或最具代表性的模型。例如：Dense 选 `Llama-3-8B`，MoE 选 `Qwen3-30B-A3B`。
    - **规模抽样**：确保覆盖不同的参数量级（如 7B、13B、30B），以捕捉显存、通信等瓶颈的变化。
    - **热点模型**：可基于业务使用频率，动态包含近期高频部署的模型。
    - 目标是将 L2 的总运行时间控制在 **30～60 分钟**。
- **参数空间**：直接复用 L3 的 JSON 配置文件，但每个模型只运行 **1～2 组最核心的参数组合**（如最大 `max-model-len` 和最大 `batch-size`），重点验证极限情况。
- **结果与门禁**：L2 的结果同样与最新的基线（由 L3 产生）对比，退化超过阈值（如 5%）则阻止合入。结果将以清晰的可视化对比形式自动评论在 PR 中。

### 3.4 L3：全面回归与基线管理（Comprehensive Benchmark）

- **定位**：终极性能验证层，负责长期趋势监控、全量基线更新和外部对比。
- **触发方式**：
    - **定时运行（核心）**：每日或每 12 小时在 `main` 分支上自动执行。
    - **手动触发**：通过 PR 打上 `nightly-test` 标签并配合 `/nightly` 评论命令，可在 PR 分支上提前运行全量或部分 L3 测试。
    - **不在合并时触发**：为了节省资源并简化流程，采用“PR 门禁 + 定时基线”模式，合并不再单独触发 L3。基线的滞后由每日定时任务弥补，对绝大多数场景足够。
- **覆盖范围**：包含项目目标支持的全部模型（Dense、MoE、多模态等），按参数量分级并分配对应 NPU 数量。参数遍历空间需合理设计，避免组合爆炸；单次测试的迭代次数和请求数沿用 vllm-ascend 的固定配置。
- **任务并行与容错**：L3 测试矩阵可按模型或参数维度拆分为多个独立 Job，利用 GitHub Actions 的 `matrix` 策略并行执行。单个 Job 失败后应自动重试 1 次，避免因环境波动导致整体失败。
- **基线更新**：每次 L3 成功完成后，将吞吐、延迟等关键指标汇总输出为 `baseline.json`，并上传至统一存储位置（如对象存储、仓库 `benchmarks/baselines/` 目录），供 L1、L2 门禁动态拉取。

### 3.5 关于“PR 触发”与“合并触发”的取舍

vLLM 社区在 PR 打标签时和合并后都会触发性能测试。前者是**门禁**，后者是**基线更新**。在本方案中，我们采纳简化策略：**仅保留 PR 门禁，不进行合并后触发**。基线更新完全由定时 L3 任务负责。其代价是基线可能存在数小时滞后，性能劣化的定位也可能稍有延迟，但这显著节省了 NPU 资源并简化了流程。对于对实时性要求极高的项目，可保留合并后触发，但需额外资源投入。

### 3.6 标签触发与评论命令机制

参考 vllm-ascend 的实践，使用“评论指令 + 标签”的组合来实现按需、受控的触发：

1. 开发者在 PR 下发评论，如 `/perf-test llama-3-8b`，指定需要运行的测试子集。
2. 有权限的成员审查代码后，为 PR 打上对应的触发标签（如 `perf-benchmarks` 或 `nightly-test`）。
3. GitHub Actions 工作流被标签事件触发，解析评论中的命令，仅执行对应测试。
4. 该机制天然形成权限审批：任意贡献者可申请测试，但只有具备写权限的成员才能通过打标签真正触发资源消耗。

注意：**必须先发评论，再打标签**，否则工作流无法读取到命令参数。

### 3.7 定期评估与校准机制（新增）

为保证各层测试矩阵的持续有效性，需引入定期评估流程：

- **频率**：每月一次，或每当有重大模型引入时。
- **评估内容**：
    - L1 的代表性小模型是否仍能代表主流架构？是否遗漏了新关键算子？
    - L2 的抽样模型是否仍能覆盖所有活跃架构和规模？运行时间是否超限？
    - L3 的模型矩阵是否应增减模型以跟上业务需求？
- **输出**：更新 L1/L2/L3 的模型列表与参数配置，并同步修改 CI 配置文件。此变更也通过 PR 流程（需相关干系人评审）进行，以确保透明和可追溯。

## 4. 性能数据稳定性保障

国产算力环境下，硬件频率波动、温度影响、算子编译缓存等因素会导致性能数据噪声。以下措施可有效提升测量可靠性：

1. **环境锁定与隔离**：
    - 使用固定的 Docker 镜像（见第 6 节），锁定 CANN、驱动、Python 依赖版本。
    - 通过 `taskset` 或 cgroup 绑定 CPU 核心，关闭动态调频。
    - 确保 NPU 独占，执行前后调用 `kill_npu_processes` 清理残留进程。
2. **测试方法标准化**：
    - 充分预热（预热 5 次，测量 15 次以上），以 P50（中位数）作为核心对比指标，辅以 P99 观察尾延迟。
    - 使用固定种子的 dummy 输入或均匀请求模式，避免真实流量随机性。
3. **自动容错**：
    - 阈值设为 5%，并支持自动重试一次，避免偶发毛刺导致误报。
    - 检测到退化时，若重试通过则自动评论“波动忽略”，否则坚决拦截。

## 5. 标签权限控制

GitHub 标签无法直接设置增删权限，需通过以下手段进行应用层或平台层控制：

- **工作流内鉴权**：在 Benchmark 作业的第一步检查 `github.actor`（打标签者）是否在预定义的授权列表中，若否，则直接使作业失败，阻止后续步骤执行。
- **评论指令 + 双保险**：结合第 3.5 节的评论命令机制，工作流同时验证评论发布者身份，仅当打标签者和命令发出者均为授权用户时才执行。
- **仓库规则集（推荐）**：对于使用 GitHub Team/Enterprise 的团队，可启用 Rulesets，限制只有特定角色或团队才能添加/删除 `perf-benchmarks`、`nightly-test` 等高权限标签，从平台层彻底杜绝越权触发。
- **Environment 保护（额外加固）**：可将性能测试 Job 关联到一个受保护的 GitHub Environment，该 Environment 可配置 **Required reviewers** 或 **Wait timer**，只有指定审批人或等待一定时间后，流水线才真正运行。这为资源消耗较大的 L2/L3 手动触发提供额外安全性。

建议采用“规则集 + 工作流鉴权，高风险测试再加 Environment 审批”的立体防护策略。

## 6. 环境标准化：Docker 镜像实践

为避免依赖漂移导致的“概率性失败”，需构建并使用预置的固定 Docker 镜像：

1. **基础镜像选择**：优先使用昇腾官方提供的带 CANN 和 NPU 驱动的镜像，如 `quay.io/ascend/vllm-ascend:v0.11.0rc0`，并**使用完整版本号或 SHA256 摘要**拉取，严禁使用 `latest`。
2. **定制化构建**：
    - 编写专用 Dockerfile，先 `COPY requirements.txt` 后 `RUN pip install`，利用缓存加速。
    - 在 `requirements.txt` 中冻结所有 Python 包版本（如 `torch==2.1.0`）。
    - 不将模型权重和 NPU 驱动打包进镜像，运行时通过 `-v` 挂载。
3. **CI 集成**：为 CI 构建专用镜像（如 `Dockerfile.ci`），可嵌入固定的测试脚本和工具。镜像标签可包含 commit hash，确保环境与代码严格对应。
4. **多平台支持**：若需覆盖不同硬件或操作系统，可采用构建矩阵生成多款镜像。

## 7. 实施路线图

| 阶段 | 时间 | 关键任务 | 触发与说明 |
| :--- | :--- | :--- | :--- |
| **第一阶段** | 1-2 周 | 搭建 L1 门禁 | PR 自动触发，实现基线对比，配置路径过滤和权限控制 |
| **第二阶段** | 2-4 周 | 构建 L3 全面回归 | 定时触发（主分支），实现全量矩阵、基线产出、看板和告警 |
| **第三阶段** | 2-3 周 | 建设看板与自动化告警 | 依赖 L3 数据，完成 Dashboard、自动通知与 Issue 创建 |
| **第四阶段** | 3-4 周 | 建设 L2 专项验证 | 依赖 L3 的模型矩阵与基线，实现代码路径自动触发+评论手动触发 |
| **第五阶段** | 持续 | 定期评估与校准 | 每月或按需评审并更新测试矩阵，纳入标准变更流程 |

## 8. 结语

本方案通过借鉴 vLLM 和 vllm-ascend 的成熟实践，构建了一套“**轻量门禁（L1）+ 按需专项（L2）+ 全面回归（L3）**”的三层 Benchmark 体系，并辅以环境强控制、统计容错和灵活的权限管理。在国产算力复杂硬件环境下，该体系既能提供 PR 级别的快速性能反馈，又能通过定时全面测试守护长期性能基线，同时借助 L2 层在效率与覆盖率之间取得动态平衡，确保每一次性能敏感的变更都得到充分验证，为推理引擎的持续性能优化提供坚实支撑。
